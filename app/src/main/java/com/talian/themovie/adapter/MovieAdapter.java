package com.talian.themovie.adapter;

import android.content.Context;
import android.widget.ListView;

public class MovieAdapter extends ListView {
    public MovieAdapter(Context context) {
        super(context);
    }
}
